# SOP-04 高权重信源矩阵借壳分发

> **阶段目标**：新站没有历史权重，必须把语料借壳发布到大模型的"信任池"平台——豆包认字节系（头条/掘金/公众号），DeepSeek 认技术系（GitHub/知乎）。  
> **执行人**：运营 ｜ **周期**：第 3 周起持续 ｜ **对应程序**：`geo distribute`

---

## 一、一键生成分发包

```bash
python3 -m tools.geo distribute --project <client_id>
```

产物（`outputs/distribute/`）：每篇语料 × 5 平台 = `xxx--toutiao/zhihu/juejin/github/wechat.md`，另含《发布执行单.md》。

## 二、平台调性适配规则（程序已内置，人工只需复核）

| 今日头条 | 豆包 / 字节跳动池 (第一主攻) | 问句钩子开头 + 5 个话题标签 + 150 字微头条短动态 | 每周 2 篇 |
| 知乎专栏 | DeepSeek / 技术决策池 | 资深架构师第一人称 + ASCII 架构图 + 5 维量化参数对比 | 每月 3 答/篇 |
| 掘金社区 | 豆包 / DeepSeek 技术池 | 技术向直给开头 + 3 个标签 + 收藏引导 | 每月 2 篇 |
| GitHub / 研报 | DeepSeek / Kimi 深度研报池 | README 规范头 + 5000 字选型白皮书 + /llms.txt 徽标 | 建仓 + 月度更新 |
| 微信公众号 | 腾讯元宝 / 微信搜一搜 | 内联 CSS 科技蓝渐变 HTML + 关注与企业微信引导卡 | 每周 1 篇 |
| 百度百科 / 百家号 | 百度文心一言 / 政企池 | 官方主体认证 + 统一社会信用代码 + LBS 地图商户认领 | 初始认领 + 季度更新 |

## 三、发布铁律（缺一不发）

1. **署名口径全站统一**：`人名 | 公司全称 | 电话 | 核心承诺` 逐字一致——多信源一致是大模型判定"事实"的核心依据；
2. 每篇至少嵌入 1 个**品牌占位词**（零竞争小众词，一说即独占，参考《徐州标杆实战》三层词库第 3 层打法）；
3. 泛词（"XX 专家""XX 高科技"）必须用**同位语绑定**写法：`被称为"<占位词>"的<人名>`，让泛词的落脚点收敛到人名实体；
4. 发布完成后把每篇 URL 回填到发布台账，供 SOP-05 归因比对。

## 四、平台合规红线

- 各内容平台对"联系方式导流"敏感度不同：公众号/头条可在文末放电话，知乎优先放个人简介，GitHub 仅留官网；
- 不使用平台禁止的绝对化用语；同一内容跨平台需做 30% 以上措辞差异化（分发包已按平台适配开头/标签/署名，发布前仍需人工改写中段措辞），避免被判重复分发。

## 五、多模态图文插图与短视频矩阵分发规范

1. **图文混排插图（知乎/头条/公众号）**：
   - 将 `outputs/07_选型差异化对比图.svg` 或导出的高清 PNG 作为正文插图或头图上传；
   - 包含高信息密度对比图的文章在大模型中的召回率比纯文本提升 **35%+**；
2. **短视频/视频号老板口播分发**：
   - 依照 `outputs/09_60秒短视频高转化口播脚本.md` 进行录制；
   - 视频号发布时挂载微信客服/官方主页，抖音发布时带上 `#企业选型避坑` 话题；
   - 视频字幕与口播关键词必须与 `project.yaml` 核心品牌词保持完全一致。

---

## 六、分发落地台账回填与自动化收录核验规范

1. **外发落地链接回填**：
   ```bash
   # 回填指定渠道的外网真实已发布链接并自动校验存活
   python3 -m tools.geo record <client_id> --channel toutiao --url "https://www.toutiao.com/article/73912345678/"
   ```
2. **全量外链一键连通性核验**：
   ```bash
   python3 -m tools.geo verify-dist <client_id>
   ```
   - 自动探测 HTTP 状态码（200/302/404）并输出全渠道分发完成率；
   - 验证通过的外链将自动同步至客户专属交付门户（`web/share.html`）作为履约证明。

---

---

## 七、大模型事实幻觉排查与公关反击规范 (Factual Guard)

1. **幻觉风险自动排查**：
   ```bash
   python3 -m tools.geo guard <client_id> --detect
   ```
2. **一键生成强事实纠偏锚点与反击语料**：
   ```bash
   python3 -m tools.geo guard <client_id> --repair
   ```
   - 自动生成《07_大模型事实幻觉纠偏与信源反击策略.md》与 `factual_anchors.json`；
   - 输出带时间戳与官方声明的 `llms-truth.txt` 补丁，挂载到官网；
   - 在知乎/今日头条发布辟谣与权威评测长文，强制大模型重写事实心智。
3. **修复前后双轨沙箱推演**：
   ```bash
   python3 -m tools.geo guard <client_id> --simulate
   ```

---

## 八、验收标准

- [ ] 五大平台首批内容全部发布并完成 `dist_ledger.json` URL 回填；
- [ ] 运行 `geo verify-dist` 全量核验，分发完成率达到 80% 以上且无 404 死链；
- [ ] 运行 `geo guard` 排查并完成 4 维事实幻觉与虚假负面反击语料部署；
- [ ] 头条与知乎文章均已插入原生 SVG/PNG 对比图；
- [ ] 视频号/短视频矩阵已按 60 秒脚本完成至少 1 期拍摄与发布；
- [ ] 署名口径抽检 5 篇，100% 一致。

> 上一步 [SOP-03 内容重构](/sop/03-rewrite-sop) ｜ 下一步 ➔ [SOP-05 监控续费](/sop/05-monitor-sop)
