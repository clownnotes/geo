# Tasks: 发前对照卡 Git 式逐条确认

## 1. 产品语义对齐

- [x] 1.1 核对并修正 `apply_corpus_diff_decision`：新增/删除/变更的 accept=固定「现在」、reject=恢复「原先」
- [x] 1.2 对照卡文案去掉「先去真相源仲裁」；改为「在本卡打勾固定」
- [x] 1.3 明确「全部确认」闸门规则（无 hard conflict、无未拍板 blocking）

## 2. API

- [x] 2.1 保留并文档化 `GET corpus/diff` 的 `fact_actions`
- [x] 2.2 收口 `POST corpus/diff-decide` 与单测
- [x] 2.3 新增 `POST corpus/diff-confirm-all`（或等价）：返回 `ready_to_pin`

## 3. 前端对照卡

- [x] 3.1 行样式：新增绿 / 删除灰 / 变更旧划线+新值；主按钮 ✓ 打勾固定
- [x] 3.2 底部「全部确认」；通过前钉住禁用或强提示
- [x] 3.3 全部确认成功后再引导钉住；钉住后清空本轮 UI 态
- [x] 3.4（可选）✗ 恢复原先，作为次要操作

## 4. 真相源轻提醒

- [x] 4.1 真相源标题旁角标/旁注：有冲突或待确认时提示「请到下方对照卡处理」
- [x] 4.2 不改真相源下拉仲裁主流程（可保留作逃生舱，不作为主文案）

## 5. 文档与验证

- [x] 5.1 更新 `docs/sop/03-rewrite-sop.md` 发前对照步骤
- [x] 5.2 单测：打勾删除解除 block；打勾变更；全部确认后可 pin
- [x] 5.3 本地 NextGEO：刷新对照 → 逐条打勾 → 全部确认 → 钉住 → 再刷新无阻塞（产品确认可归档）
