## 1. 后端服务与鉴权中心开发 (`tools/geo/server.py`)

- [x] 1.1 实现轻量级安全 Web 后端框架与 Session/Token 登录鉴权拦截器（支持环境变量配置与敏感数据隔离保护）。
- [x] 1.2 编写项目管理 RESTful API（项目列表、创建项目、获取配置、修改词库）。
- [x] 1.3 编写 5 阶段 SOP 生产流水线调度接口（调用 audit/scaffold/rewrite/distribute/monitor 并返回实时生成状态与文件内容）。
- [x] 1.4 编写交付产物在线读取与 ZIP 一键打包下载接口。

## 2. 现代化前端管理控制台开发 (`web/index.html`)

- [x] 2.1 编写登录鉴权界面与前端状态机（未登录自动拦截、登录成功跳转、安全登出）。
- [x] 2.2 编写客户项目中心总览面板（数据大盘、项目列表、快速搜索、“+新建项目”向导弹窗）。
- [x] 2.3 编写 5 步向导式 SOP 生产流水线主工作台（步骤条、一键运行按钮、加载动效、Markdown 实时渲染器与代码高亮复制）。
- [x] 2.4 编写 Step 1~5 各面板专门交互（Step 1 体检报告阅读、Step 2 底座代码预览、Step 3 语料编辑、Step 4 渠道分发卡片、Step 5 SOV 声量看板）。

## 3. CLI 启动命令集成与自动化联动

- [x] 3.1 在 `tools/geo/cli.py` 中增加 `geo web` 子命令，支持 `./geo web [--port 8080]` 快捷启动服务。

## 4. 全流程实测与体验验证

- [x] 4.1 启动 Web 页面，进行管理员登录认证测试。
- [x] 4.2 通过 Web 管理端新建客户项目，按向导一键跑完 Step 1 至 Step 5 完整 SOP 生产流水线。
- [x] 4.3 验证产物生成、一键复制、下载导出及数据安全性。

## 5. 生产上线与后续演进待办 (Future Backlog)

- [ ] 5.1 线上公网部署：腾讯云/Mac Mini 域名解析与 Nginx 反向代理配置（支持 SSL HTTPS 与公私路由隔离）。
- [ ] 5.2 进程长驻守护：配置 systemd / supervisor / launchd 生产级自启守护。
- [ ] 5.3 商业大模型全面接驳：后期接入 DeepSeek / 字节火山方舟商用付费 API，开启 100% 动态深度思考与实时在线探测。



