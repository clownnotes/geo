# Tasks: 管理端功能解答麦肯锡模型与阶段零重构

## 规范沉淀与全局约束
- [x] 1. 新建 `docs/specs/ui-feature-explanation-standard.md`：V-W-W-H、四色语义、文案自检清单；主色引用 `geo-admin-ui-tokens.md` 的 `--geo-primary`；清单含 plain-speak「阶段零两件东西」禁混谈条款
- [x] 2. 在 `AGENTS.md` 第 3 节追加专章，固定此套标准，并交叉引用 plain-speak 对照表

## 阶段零打样实战落地
- [x] 3. 重构 `Step0Header.vue`：静态区改为 V-W-W-H 四层 + 四色侧彩条；保留右上角双徽章与状态芯片；`modeBanner` 仍动态展示（首轮/再测），不得吃掉
- [x] 4. 按 design 精编文案调整 `plainCopy.js` / `useStep0.js` 联动句（若有）；零「底牌报告/基线/剧本」主文案；How 三步与页内 1/2/3 步对齐
- [x] 5. 执行 `npm run build` 重新编译 `web/assets/step0/step0.js`

## 本地回归验证与核验
- [x] 6. 本地 `python3 -m tools.geo web --port 8088`，用本机 **Safari** 打开 `http://127.0.0.1:8088` 验证阶段零排版与交互（禁 Cursor 内置浏览器）
- [x] 7. 核验：0 处红色滥用、0 处低幼 Emoji、V-W-W-H 层次清晰、两件东西文案不混谈、双徽章与 modeBanner 仍可用

## 审查返工（代码缺陷）
- [x] 8. **回滚** `tools/geo/server.py` 中未登录放行 `/assets/**` 与 `/geo-admin.css` 的改动；冒烟须带登录态，不得为过测拆门；对照 `2026-09-19-纯内部未登录代码物理隔离`
- [x] 9. 将 `docs/specs/ui-feature-explanation-standard.md` 架构图 How 第三步「入库」统一为「确认写入」（与 design / AGENTS 一致）
- [x] 10. （可选）What / Why 序号胶囊勿同用字母 `W`，改为 `2`/`3` 或「什」「为」，避免两层同标

## 阶段零架构终极演进（方案 C：IDE 专业交付工作台）
- [x] 11. 顶栏重构：彻底理顺两张交付物验收单（提问清单已就绪 vs 豆包答案待存入），操作按钮与状态彻底分离。
- [x] 12. 方案 C 原型落地：在临时前端中实现三栏工作台（左侧资源管理器文件树 + 中间宽屏多 Tab 人话纯文本编辑器 + 右侧 SOP 流程联动面板）。
- [x] 13. 编辑器核心能力闭环：支持带行号大白话排版、打磨修改、`[保存文件]` 物理落盘至 projects.json、`[撤回修改]`、`[一键复制内容]`、底部状态栏。
- [x] 14. 两级软删除安全机制（前台软删除隐藏 + 后台管理员真删与恢复）落地前台原型，并确认为生产阶段待办规范。
- [x] 15. （生产阶段推进待办）方案 C 三栏工作台与两级软删除安全底座已在原型沙盒中完成闭环验证，规格标准已确立，主仓回迁任务已登记至下一期生产变更规划。

