## 1. 规范与壳层

- [x] 1.1 登录后引入与流水线一致的全局 AdminShell（220px 风琴侧栏 + 顶栏），默认 `home-dashboard`。
- [x] 1.2 侧栏一级：仪表盘 / 企业管理 / 商业洞察 / 运维告警 / 系统设置；视觉只用 `geo-*`。
- [x] 1.3 拆除原首页顶栏胶囊堆；对应能力挂到商业洞察或运维告警。

## 2. 仪表盘

- [x] 2.1 保留硬指标：托管数、交付完成、组合价值、组合 ROI（可点进商业洞察大盘）。
- [x] 2.2 巡检/告警摘要条（无 Emoji）；详情入口指向运维告警。
- [x] 2.3 （可选）「需关注」短列表：未完成 SOP 或告警项目，跳企业管理或进流水线。

## 3. 企业管理

- [x] 3.1 迁入客户项目表、新建、批量并发、进流水线、打包/删除。
- [x] 3.2 搜索框：名称 / ID / 域名实时过滤。
- [x] 3.3 筛选：行业、SOP 完成状态、合作方（含「未分配」）。
- [x] 3.4 表格展示合作方列；支持改挂 `partner_id`。
- [x] 3.5 `backToDashboard` 语义改为返回企业管理（或提供双入口，默认企业管理）。

## 4. 商业洞察 / 运维 / 设置

- [x] 4.1 商业洞察页：EDI、Pitch、全域大盘、行业对标、集团矩阵、ROI、AI 沙箱入口卡片。
- [x] 4.2 运维告警页：巡检状态全文、全量巡检、告警与通知设置。
- [x] 4.3 系统设置：大模型中枢、离线导出；合作方名册管理（增改/归档）。

## 5. 代理合作数据

- [x] 5.1 新增 `config/geo_partners.yaml` 与 GET/POST/PATCH `/api/partners`。
- [x] 5.2 项目列表与创建/更新支持 `partner_id`；`project.yaml` 可选字段文档化。
- [x] 5.3 存量无字段视为未分配；归档合作方时清空或提示改挂。

## 6. 验证

- [x] 6.1 本地 `:8088`：登录→仪表盘无客户全表；企业管理搜索/筛选可用；改归属后刷新仍在。
- [x] 6.2 进流水线→返回落点正确；商业洞察能打开原 Modal。
- [x] 6.3 勾选 tasks；`review-log` 记 apply 结论；**停步等验收，不归档**。
- [x] 6.4 审查建议：YAML 值转义、中文 ID 随机后缀、`tests/test_partners.py` 离线回归。
