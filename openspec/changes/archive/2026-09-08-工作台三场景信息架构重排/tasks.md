# Tasks: 工作台三场景信息架构重排

## 1. 场景壳与向导顶栏

- [x] 1.1 在 `web/index.html` 向导区增加三场景 Tab（初步交付 / 二次提升 / 日常维护）与 `#scene-delivery` / `#scene-boost` / `#scene-ops` 三面板外壳；实现 `switchScene`，默认进入初步交付。
- [x] 1.2 收敛向导 header：移除与场景冲突的彩色工具堆；「一键跑通 5 步」「打包导出」保留在交付场景可达处；分享链接主入口归日常维护（交付场景可保留弱入口或仅 Ops）。

## 2. 初步交付：5 步主路径瘦身

- [x] 2.1 步骤 1：文案对齐「测算诊断」；页内只保留执行体检 + 预览；竞对/Pitch/词库等入口迁出。
- [x] 2.2 步骤 2–3：每步显眼操作不超过约 3 个；意图拓扑/图谱/RAG/视觉等迁入二次提升；上线说明类收进折叠区。
- [x] 2.3 步骤 4：四色摘要改为进度跳转；每渠道卡仅「生成包 + 复制主文」外露，「更多」折叠附属操作与 Clean MD / 后台链接；合规/注入/幻觉/视觉顶栏迁出。
- [x] 2.4 步骤 5：页内只留声量监测、导出周报、结案验收；06/18–26 等入口按 design 归属表迁至 Boost 或 Ops。

## 3. 二次提升与日常维护面板

- [x] 3.1 搭建 `#scene-boost` 四主题组卡片，挂齐 design 归属表中的进阶 Modal / 函数入口（无死链）。
- [x] 3.2 搭建 `#scene-ops` 四区块（客户可见物 / 每周巡检 / 危机声誉 / 普林斯顿复检），挂齐运维入口；监测/周报/验收可与步骤 5 共用同一函数。

## 4. 仪表盘顶栏与验收

- [x] 4.1 仪表盘顶栏 18–26 等工具 pill 迁入 Boost/Ops 对应组，列表页不再铺按钮墙（售前 Pitch 等保留合理弱入口亦可）。
- [x] 4.2 本地 `http://127.0.0.1:8088` 手测：三场景切换、5 步主路径可跑、全部原 Modal 可从新入口打开、无新增 Emoji、对照 design 归属表做零丢失 checklist。
